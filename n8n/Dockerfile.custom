FROM n8nio/n8n:latest-debian

USER root

# Install Python and dependencies for Debian
RUN apt-get update && apt-get install -y --no-install-recommends python3 python3-pip python3-venv python3-docx && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

USER node
